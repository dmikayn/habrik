<h1><%= t('forms.editing_user') %></h1>

<%= render "form", user: @user %>

<br>

<div>
  <%= link_to t('forms.show_this_user'), @user %> |
  <%= link_to t('forms.back_to_users'), users_path %>
</div>
